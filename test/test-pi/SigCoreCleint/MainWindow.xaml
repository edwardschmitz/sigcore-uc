<Window x:Class="SigCoreCleint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SigCoreCleint"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="500">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <Button Content="Open" x:Name="Open" Click="Open_Click" 
                    Margin="5" Padding="4"/>
            <Button Content="Send Ping Command" x:Name="Ping"
                    Margin="5" Padding="4" Click="Ping_Click"/>
            <Button Content="Send Relay Command" x:Name="Send"
                    Margin="5" Padding="4" Click="Send_Click"/>
            <Button Content="Close" x:Name="Close"
                    Margin="5" Padding="4" Click="Close_Click"/>
        </StackPanel>
        <TextBox Grid.Row="1" Grid.Column="0" x:Name="Log"></TextBox>
    </Grid>
</Window>
